---
import { lowerHumanize } from "@/lib/utils/textConverter";
const { tags, spirits, bottles, allSpirits, allBottles } = Astro.props;
---

<div class="lg:col-4">
  <!-- spirits -->
  <div class="mb-8">
    <div class="rounded bg-theme-light p-4 dark:bg-darkmode-theme-light">
      <h4 class="my-2 text-center">
        <a href="/drinks/spirits">Spirits</a>
      </h4>
      <ul>
        {
          spirits.map((spirit: any) => {
            return (
              <li class="inline-block">
                <a
                  class="m-1 block rounded bg-white px-3 py-1 hover:bg-primary hover:text-white dark:bg-darkmode-body dark:hover:bg-darkmode-primary dark:hover:text-dark"
                  href={`/drinks/spirits/${spirit}`}
                >
                  {lowerHumanize(spirit)}
                </a>
              </li>
            );
          })
        }
      </ul>
      <hr class="my-4">
      <!-- bottles -->
      <h4 class="my-2 text-center">
        <a href="/drinks/bottles">Bottles</a>
      </h4>
      <ul>
        {
          bottles.map((bottle: any) => {
            return (
              <li class="inline-block">
                <a
                  class="m-1 block rounded bg-white px-3 py-1 hover:bg-primary hover:text-white dark:bg-darkmode-body dark:hover:bg-darkmode-primary dark:hover:text-dark"
                  href={`/drinks/bottles/${bottle}`}
                >
                  {lowerHumanize(bottle)}
                </a>
              </li>
            );
          })
        }
      </ul>
      <hr class="my-4">
      <!-- tags -->
      <h4 class="my-2 text-center">
        <a href="/drinks/tags">Tags</a>
      </h4>
      <ul>
        {
          tags.map((tag: any) => {
            return (
              <li class="inline-block">
                <a
                  class="m-1 block rounded bg-white px-3 py-1 hover:bg-primary hover:text-white dark:bg-darkmode-body dark:hover:bg-darkmode-primary dark:hover:text-dark"
                  href={`/drinks/tags/${tag}`}
                >
                  {lowerHumanize(tag)}
                </a>
              </li>
            );
          })
        }
      </ul>
    </div>
  </div>
</div>
